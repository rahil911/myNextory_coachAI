bd show baap-xbu && bd update baap-xbu --status=in_progress && work on it. CRITICAL: (1) The Path tab already has a skeleton in tory-workspace.js — find the renderPathTab or similar function and fill it in with the 4-column kanban. (2) Reuse the DnD pattern from kanban.js:140-217 (dragstart/dragover/drop/getDragAfterElement). (3) 4 columns: Available Pool, Discovery, Main Path, Completed. (4) Lesson cards show: title, journey badge, match_score, difficulty dots. (5) Impact preview panel appears on drag — shows gap coverage, path balance, journey mix changes. (6) 'Apply Change' commits via the reorderToryPath/swapToryLesson/lockToryRecommendation API calls from api.js. (7) Data comes from getToryUserDetail which returns the learner's path + available lessons. (8) DO NOT rewrite tory-workspace.js — ONLY modify the path tab section. (9) Register ownership for any new files.

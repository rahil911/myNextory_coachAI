bd show baap-6iv && bd update baap-6iv --status=in_progress && work on it: Fix the import path shadowing in .claude/rag/curator_prompts.py and all other .claude/rag/*.py files. The backend config.py at .claude/command-center/backend/config.py shadows the RAG config.py. Fix by using os.getenv() directly instead of importing from config where there's conflict. Then restart uvicorn with .env sourced and verify ALL AI endpoints work: curator briefing, curator chat, companion greeting, companion chat, instantiation. Test user: 200.
